import styled from "styled-components";
import { sideWidth } from "../constants";

export const SidePieceContainer = styled.div`
    width:${sideWidth}px;
    display:flex;
    align-items:center;
    justify-content:center;
`;